
const activo = true;

// let mensaje = '';

// if (activo) {
//   mensaje = 'Activo';
// } else {
//   mensaje = 'Inactivo';
// }

// Ternario
// const mensaje = ( activo ) ? 'Activo' : 'Inactivo';
// const mensaje = ( activo ) ? 'Activo' : null;

// Operador and &&
const mensaje = activo && 'Activo'; // Si es !activo => regresa false

console.log(mensaje);


